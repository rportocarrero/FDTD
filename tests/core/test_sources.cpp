#include "gtest/gtest.h"
#include "core/sources.h"

TEST(SourcesTest, AddSource) {
  // Test adding a source to the FDTD grid
  // Assuming you have a class Grid with addSource() method
  Grid grid;
  Source source; // Assuming you have a Source class
  grid.addSource(source);
  // Add assertions to check if the source was added correctly
}
